# ---------------
# --- NETWORK ---
# ---------------
networks:
  hexagon-network:
    driver: bridge

# ---------------
# --- VOLUMES ---
# ---------------
volumes:
  hexagon_db_data:
  hexagon_mongo_data:

# ----------------
# --- SERVICES ---
# ----------------
services:
  # ----------------
  # --- POSTGRES ---
  # ----------------
  hexagon_db:
    image: postgres:latest
    container_name: hexagon_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: hexagon_db
    volumes:
      - hexagon_db_data:/var/lib/postgresql
    networks:
      - hexagon-network
    ports:
      - "5432:5432"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  # ---------------
  # --- MONGODB ---
  # ---------------
  hexagon_mongo:
    image: mongo:latest
    container_name: hexagon_mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
      MONGO_INITDB_DATABASE: hexagon_mongo
    volumes:
      - hexagon_mongo_data:/data/db
    networks:
      - hexagon-network
    ports:
      - "27017:27017"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  # ---------------
  # --- HEXAGON ---
  # ---------------
  hexagon_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hexagon_app
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: hexagon_db
      POSTGRES_DATABASE: hexagon_db
    ports:
      - "8080:8080"
    depends_on:
      - hexagon_db
    networks:
      - hexagon-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"